# QR Code Converter — 설계 문서 (PLAN)

엑셀 파일에 정리된 URL 목록을 읽어 일괄로 QR 코드 이미지로 변환·저장하는 Windows 데스크톱 유틸리티.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 프로그램명 | QR Code Converter |
| 배포 형태 | 단일 실행 파일 (`.exe`, 설치 불필요) |
| 대상 OS | Windows 10/11 |
| 입력 | 엑셀 파일 (`.xlsx`) — `Source URL`, `QR Code Name` 2개 컬럼 |
| 출력 | QR 코드 이미지 파일 (`.png` 기본 / `.jpg` 선택) + 실행 로그 파일 |

## 2. 기술 스택

간편한 유틸 목적에 맞춰 **Python 단일 스택**으로 구성한다. 모든 라이브러리가 순수 Python 또는 Pillow 기반이라 PyInstaller 단일 exe 빌드가 안정적이다.

| 역할 | 선택 | 선택 이유 |
|---|---|---|
| 언어 | Python 3.11+ | 빠른 개발, exe 빌드 용이 |
| GUI | tkinter (표준 라이브러리) | 추가 의존성 없음, exe 용량 최소화, 이 정도 UI에 충분 |
| QR 생성 | `qrcode` + `Pillow` | 사실상 표준 QR 라이브러리, PNG/JPG 저장 지원 |
| 엑셀 읽기/쓰기 | `openpyxl` | 템플릿 생성과 데이터 읽기 모두 처리 |
| exe 빌드 | `PyInstaller` | `--onefile --windowed` 로 단일 exe 생성 |

> PySide6/Qt는 exe 용량(80MB+)과 라이선스 고려가 필요하므로 제외. tkinter로 충분.

## 3. 엑셀 데이터 스펙

### 3.1 템플릿 형식 (`QR_Template.xlsx`)

| 컬럼 (1행 헤더) | 설명 | 예시 |
|---|---|---|
| `Source URL` | QR 코드로 변환할 URL | `https://example.com/page1` |
| `QR Code Name` | 저장할 이미지 파일명 (확장자 제외) | `event_page_01` |

- 1행은 헤더 고정, 2행부터 데이터.
- 템플릿에는 예시 데이터 1~2행을 포함해 사용자가 형식을 바로 이해할 수 있게 한다.

### 3.2 읽기 규칙

- 첫 번째 시트만 읽는다.
- 헤더명 기준으로 컬럼을 찾는다(컬럼 순서 무관, 대소문자·공백 관대하게 매칭).
- `Source URL` 또는 `QR Code Name`이 비어 있는 행은 스킵하고 로그에 경고를 남긴다.
- 파일명에 사용할 수 없는 문자(`\ / : * ? " < > |`)는 `_` 로 치환한다.
- 총 데이터 건수(m)는 **작업 시작 전에 미리 계산**하여 진행률 표시에 사용한다.

## 4. 화면 설계

화면은 2개. 하나의 창(고정 크기, 약 560×480)에서 프레임 전환 방식으로 구현한다.

### 4.1 화면 1 — 설정 화면 (메인)

```
┌──────────────────────────────────────────────┐
│  QR Code Converter                            │
├──────────────────────────────────────────────┤
│  엑셀 파일:   [경로 표시            ] [찾아보기] │
│               [ 엑셀 템플릿 다운로드 ]           │
│  ※ 유의사항: QR Code 이름 중복이 없도록 주의!    │
│                                              │
│  QR 저장 경로: [경로 표시           ] [폴더 선택] │
│                                              │
│  저장 확장자:  [ PNG          ▼ ]             │
│                                              │
│  ┌─ QR 속성 ──────────────────────────────────┐ │
│  │  QR 크기(box_size): [ 50 ] (기본값 50, 1~50) │ │
│  │  테두리(border):    [ 1 ]  (기본값 1, 0~20)  │ │
│  └────────────────────────────────────────────┘ │
│                      [ 실행 ]   [ 닫기 ]      │
└──────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| 엑셀 파일 찾아보기 | 파일 탐색기(`filedialog.askopenfilename`, `*.xlsx` 필터) |
| 엑셀 템플릿 다운로드 | 저장 위치 선택 다이얼로그(`asksaveasfilename`) → `QR_Template.xlsx` 생성 |
| 유의사항 문구 | 엑셀 첨부 영역 근처에 고정 라벨 표시: "※ 유의사항: QR Code 이름 중복이 없도록 주의!" (강조 색상, 예: 빨강/주황) |
| QR 저장 경로 폴더 선택 | 폴더 탐색기(`filedialog.askdirectory`) |
| 저장 확장자 | 콤보박스(`ttk.Combobox`, 읽기 전용) — 항목: `PNG`, `JPG`, 기본값 **PNG** |
| QR 속성 영역 | `ttk.LabelFrame`("QR 속성")으로 구분된 박스. 크기/테두리를 각각 별도 줄에 배치하고, 각 항목 옆에 기본값·입력 범위 표기 |
| QR 크기 (box_size) | 숫자 입력(`ttk.Spinbox`, 1~50), 기본값 **50** — QR 모듈 1칸의 픽셀 크기. 옆에 "(기본값 50, 입력 범위 1~50)" 표기 |
| QR 테두리 (border) | 숫자 입력(`ttk.Spinbox`, 0~20), 기본값 **1** — QR 주변 여백 모듈 수. 옆에 "(기본값 1, 입력 범위 0~20)" 표기 |
| 실행 | 입력 검증(엑셀 파일 존재, 저장 폴더 지정 여부) 통과 시 화면 2로 전환 후 작업 시작. 실패 시 경고 팝업 |
| 닫기 | 프로그램 종료 |

### 4.2 화면 2 — 진행 화면

```
┌──────────────────────────────────────────────┐
│  변환 진행                                    │
├──────────────────────────────────────────────┤
│  [███████████░░░░░░░░░░░░]      12 / 30      │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │ [1] Start convert URL to QR Code -     │  │
│  │     https://... to event_01            │  │
│  │ [1] Convert Success : event_01         │  │
│  │ [2] Start convert URL to QR Code - ... │  │
│  │ ...                       (스크롤 가능)  │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  완료: 30개 중 29개 성공 (실패 1개)             │
│  로그 저장 위치: C:\...\log\20260707_153012.log │
│                                              │
│                              [ 닫기 / 완료 ]  │
└──────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| Progress bar | `ttk.Progressbar`, 시작 전 파악한 총 건수(m) 기준 |
| 진행 개수 | `n / m` 텍스트, 1건 처리마다 갱신 |
| 로그 영역 | 넓은 `Text` 위젯(읽기 전용, 스크롤바), 처리 로그 실시간 출력 |
| 요약 영역 | 작업 완료 시에만 표시: `m개 중 n개 성공` + 로그 파일 저장 위치 |
| 닫기 버튼 | 아래 4.3 상태별 동작 참조 |

### 4.3 닫기/완료 버튼 상태 정의

| 작업 상태 | 버튼 라벨 | 클릭 시 동작 |
|---|---|---|
| 진행 중 | `닫기` | **작업 일시정지** → "정말 작업을 취소하시겠습니까?" (예/아니오) 팝업<br>예: 작업 중단 후 프로그램 종료 (중단 시점까지의 로그는 파일 저장)<br>아니오: 팝업 닫고 **작업 재개** |
| 완료 후 | `완료` | 프로그램 종료 |

핵심 요구사항: **확인 팝업이 떠 있는 동안 변환 작업은 멈춰 있어야 한다.** (구현 방식은 6.2 참조)

## 5. 로그 설계

### 5.1 로그 포맷

```
[{n}] Start convert URL to QR Code - {url} to {QR Code Name}
[{n}] Convert Success : {QR Code Name}
[{n}] Convert Failed : {QR Code Name} - {오류 사유}     ← 실패 시
```

- `n`은 1부터 시작하는 처리 순번. 각 줄은 줄바꿈으로 구분.
- 화면 Text 영역과 로그 파일에 동일한 내용을 기록한다.

### 5.2 로그 파일 저장

- 위치: **exe 실행 파일과 같은 레벨**의 `log` 폴더 (없으면 자동 생성)
  - PyInstaller onefile 환경에서는 `sys.executable` 기준 경로 사용 (`sys._MEIPASS`/cwd 아님에 주의)
  - 개발(스크립트) 실행 시에는 스크립트 위치 기준으로 폴백
- 파일명: timestamp 기반 — `YYYYMMDD_HHMMSS.log` (예: `20260707_153012.log`)
- 저장 시점: 작업 시작 시 파일을 열고 로그 발생 즉시 append(중간 취소에도 로그 보존)
- 마지막에 요약(성공/실패 건수) 도 파일에 기록

## 6. 동작 흐름 및 스레딩 설계

### 6.1 전체 흐름

```
[화면 1] 엑셀 선택 → 저장 폴더 선택 → 확장자 선택 → [실행]
   │  (검증: 파일 존재? 폴더 지정? 엑셀 파싱 가능? 데이터 0건 아님?)
   ▼
[화면 2 전환] 엑셀 로드 → 총 건수 m 확정 → 로그 파일 생성 → 워커 스레드 시작
   │
   ▼  (행마다 반복)
   Start 로그 → qrcode 생성 → {저장경로}\{QR Code Name}.{png|jpg} 저장
   → Success/Failed 로그 → progress n/m 갱신
   │
   ▼
완료 → 요약 표시("m개 중 n개 성공", 로그 위치) → 버튼 라벨 [닫기]→[완료]
```

### 6.2 스레딩 모델

tkinter UI 프리징을 막기 위해 변환 작업은 **워커 스레드**에서 수행한다.

- **UI 스레드**: 화면 갱신 전담. 워커가 넣은 이벤트를 `queue.Queue` + `root.after(100ms)` 폴링으로 수신해 로그/progress 갱신 (tkinter 위젯은 UI 스레드에서만 조작).
- **워커 스레드**: 행 단위로 QR 생성·저장. 매 건 처리 전에 `threading.Event` 두 개를 확인:
  - `pause_event`: **닫기 클릭 → 확인 팝업 표시 직전에 clear** → 워커는 `pause_event.wait()`로 대기(= 작업 멈춤). "아니오" 선택 시 set → 재개.
  - `cancel_event`: "예" 선택 시 set → 워커는 루프 탈출, 로그 파일 마무리 후 종료.
- 파일명 중복 시 처리: 저장 시점에 같은 이름의 파일이 이미 존재하면 `파일명(2)`, `파일명(3)`… 순으로 번호를 붙여 저장한다 (Windows 탐색기 방식).
  - 예: `event_01.png`가 있으면 → `event_01(2).png`, 그것도 있으면 → `event_01(3).png`
  - 대상 폴더의 기존 파일과 이번 작업 내 중복 모두 동일하게 적용. 실제 저장된 파일명을 Success 로그에 기록한다.

### 6.3 QR 생성 파라미터

| 항목 | 값 |
|---|---|
| 오류 보정 | `ERROR_CORRECT_M` (15%) |
| box_size / border | **화면 1에서 사용자 입력** — 기본값 50 / 1 (범위 1~50 / 0~20). 실행 시 양의 정수(border는 0 허용) 검증 |
| 색상 | 흑백 (검정 모듈 / 흰 배경) |
| JPG 저장 시 | RGB 변환 후 저장 (JPG는 투명도 미지원) |

## 7. 프로젝트 구조

```
QR_Code_Converter/
├── PLAN.md                  # 본 문서
├── requirements.txt         # qrcode, Pillow, openpyxl, pyinstaller
├── build.bat                # exe 빌드 스크립트
├── src/
│   ├── main.py              # 엔트리포인트, 창 생성/프레임 전환
│   ├── ui_setup_frame.py    # 화면 1 (설정)
│   ├── ui_progress_frame.py # 화면 2 (진행/로그/요약)
│   ├── converter.py         # 워커 스레드: 엑셀 읽기 → QR 생성/저장
│   ├── excel_template.py    # 템플릿 xlsx 생성
│   ├── logger.py            # 화면+파일 동시 로그, log 폴더/timestamp 파일 관리
│   └── paths.py             # exe/스크립트 실행 경로 판별 유틸
└── dist/                    # 빌드 산출물 (QR_Code_Converter.exe)
```

> 규모가 작으므로 단일 `main.py`로 합쳐도 무방하나, 화면/로직 분리를 위해 위 구조를 권장.

## 8. 빌드

```bat
pyinstaller --onefile --windowed --name QR_Code_Converter src\main.py
```

- `--onefile`: 단일 exe
- `--windowed`: 콘솔 창 숨김 (GUI 전용)
- 산출물: `dist\QR_Code_Converter.exe` — 이 파일만 배포하면 됨
- 템플릿 xlsx는 파일 동봉이 아니라 **openpyxl로 런타임 생성**하므로 리소스 번들링 불필요

## 9. 예외 처리 정책

| 상황 | 처리 |
|---|---|
| 엑셀 파일 열기 실패 / 필수 컬럼 없음 | 실행 전 검증 단계에서 경고 팝업, 화면 1 유지 |
| 데이터 0건 | 경고 팝업 후 화면 1 유지 |
| URL·이름 빈 행 | 해당 행 스킵, 로그에 경고 기록 (실패 건수에 포함) |
| 개별 QR 생성/저장 실패 | `Convert Failed` 로그 남기고 다음 행 계속 진행 |
| 저장 폴더 없음/권한 없음 | 실행 전 검증 (쓰기 테스트) |
| box_size/border 값이 숫자가 아니거나 범위 밖 | 실행 전 검증 단계에서 경고 팝업, 화면 1 유지 |
| log 폴더 생성 실패 | 경고 팝업 표시 후 화면 로그만으로 계속 진행 |

## 10. 구현 순서 (마일스톤)

1. **골격**: 창 + 화면 1/2 프레임 전환, 위젯 배치
2. **템플릿**: 엑셀 템플릿 생성/다운로드 기능
3. **핵심 로직**: 엑셀 파싱 → QR 생성/저장 (CLI 수준에서 먼저 검증)
4. **진행 화면 연동**: 워커 스레드 + 큐 기반 로그/progress 갱신
5. **일시정지/취소**: pause/cancel 이벤트, 확인 팝업, 버튼 라벨 전환
6. **파일 로그**: log 폴더 자동 생성, timestamp 파일, 요약 기록
7. **빌드/검증**: PyInstaller 빌드 후 exe 기준 경로(log 폴더 위치) 동작 확인
